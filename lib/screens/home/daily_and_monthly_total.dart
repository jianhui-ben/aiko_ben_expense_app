import 'package:flutter/material.dart';

class DailyAndMonthlyTotal extends StatelessWidget {
  const DailyAndMonthlyTotal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('placeholder for total');
  }
}
